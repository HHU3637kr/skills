# Competitive Edge

Why token-efficiency wins vs GitHub incumbents.

## Positioning one-liner

**The only audit-first, cross-agent token optimizer with ROI-ranked fixes and semantic-safe compression.**

## Beat caveman

| caveman | us |
|---------|-----|
| Output-only (~75% output) | L0–L5 full stack |
| 24KB CLAUDE.md (ironic bloat) | Skill <200 lines, on-demand load |
| Caveman speak hurts enterprise UX | Professional terse mode |
| MCP shrink middleware (npm) | Detect + advise + optional shrink path |
| Stats from Claude session only | audit.py works without any agent running |

**Take:** borrow multi-agent adapter layout + stats idea; don't copy output style as default.

## Beat Minimize-Cursor-Cost

| Minimize-Cursor | us |
|-----------------|-----|
| Static rules drop-in | Audit → fix → verify loop |
| Cursor `.mdc` centric | Core + adapters for 6+ agents |
| bash + ps1 dual scripts | Single Python install.py |
| No MCP/skill scan | Full L0 fixed-tax scan |
| Manual before/after | Automated estimate + re-audit |

**Take:** borrow agent-efficiency.mdc content structure wholesale in v0.2.

## Beat context-compress

| context-compress | us |
|------------------|-----|
| Instruction files only | + behavior + MCP + skills |
| pip install | stdlib-only scripts |
| No install emit | install.py writes adapters |
| No ROI ranking | Layer-tagged fix list |

**Take:** port ContentType classifier into compress.py.

## Beat CRUX-Compress

| CRUX | us |
|------|-----|
| 5–10x via DSL | 40–50% via plain markdown (lower risk) |
| CRUX.md spec overhead | Zero new notation |
| Cursor-focused workflow | Agent matrix |

**Take:** optional CRUX path later; not default.

## Beat gh-aw (GitHub internal)

| gh-aw | us |
|-------|-----|
| CI workflow token-usage.jsonl | Personal dev environment |
| Enterprise proxy required | Works offline with file scan |
| Optimizer opens GitHub issues | Human-readable audit report |

**Take:** borrow turn-count insight + auditor→optimizer loop pattern.

## Shipped in v0.1 (we already beat them on)

1. **audit.py** — MCP parse, skill scan, dedup, ROI fixes, agent detect
2. **compress.py** — full ContentType pipeline (context-compress grade)
3. **install.py** — 9 agents, one Python entry, Win/Mac/Linux/WSL
4. **core/** — L0+L1+L3 rules filled (Minimize-Cursor + landmines + caveman insights)

## Moats to build next

1. **Benchmark suite** — 20 repos, before/after (v1.0)
2. **Ledger adapters** — optional Phoenix / console export plugin
3. **Community lever library** — user-submitted fixes by layer + stack

## Non-goals (stay focused)

- Not a model router (L5 advise only)
- Not a prompt marketplace
- Not replacing linters/formatters
- Not caveman cosplay
