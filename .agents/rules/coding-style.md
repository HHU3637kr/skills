# 编码风格

- Skill 文件使用 Markdown 格式，遵循 Obsidian Flavored Markdown
- YAML frontmatter 只保留 `name` + `description`
- 每个 Skill 主文件 `SKILL.md` 保持精简，详情放 `references/`
- 变量/占位符使用 `{花括号}` 标记
- 文件和目录命名优先使用中文描述或 kebab-case
- 只改与当前任务相关的文件，避免无关格式化和元数据 churn
